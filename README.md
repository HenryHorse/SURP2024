# Hello World

This is a simple Hello World program in C++.
Run using the following commands to ensure usage of C++20. Ensure g++ is installed.

Compile using:
```sh
make
```

Run using:
```sh
./hello_world
```
